# 碎碎念 Suisui 项目开发记录

> 记录时间：2026年6月15日

## 项目概述

社交碎碎念 App，三端架构：
- **Android 端** (`app/`)：Kotlin/Java，Retrofit + Glide + ViewBinding
- **服务端** (`suisui-server/`)：Spring Boot 3.2.6 + MySQL + JPA + JWT
- **后端管理后台**：未开发

---

## 一、服务端搭建

### 技术栈
- Spring Boot 3.2.6 + Spring Security + JPA
- MySQL 数据库 `suisui`
- JWT 认证（jjwt 0.12.6）
- 文件上传存储到 `uploads/images/`

### 数据库（9张表）
users, notes, note_images, note_tags, comments, likes, subscriptions, blocklists, notifications

### API 端点
| 模块 | 端点 | 说明 |
|------|------|------|
| 认证 | `/api/auth/register` `/api/auth/login` `/api/auth/password` | 注册/登录/改密 |
| 用户 | `/api/user/profile` `/api/world/{id}` `/api/world/public` | 资料/世界页/公开开关 |
| 碎碎念 | `/api/notes` `/api/notes/{userId}` `/api/notes/detail/{id}` | CRUD |
| 点赞 | `/api/likes` | 点赞/取消 toggle |
| 评论 | `/api/comments` `/api/comments/{noteId}` | CRUD |
| 订阅 | `/api/subscriptions` `/api/subscriptions/{id}` | 列表/toggle |
| 黑名单 | `/api/blocklist` `/api/blocklist/{id}` | 列表/toggle |
| 通知 | `/api/notifications` | 列表/清空 |
| 统计 | `/api/stats` | 数据统计 |
| 图片 | `/api/images/upload` | 上传 |
| 发现 | `/api/world/discover` | 发现公开用户 |

### 启动方式
```bash
# 一键启动（推荐）
双击 suisui-server/start.bat

# 或手动
java -jar suisui-server/target/suisui-server-1.0.0.jar
```

### 配置
- 端口：8080
- 数据库：默认 root/123456（可用环境变量 DB_USERNAME / DB_PASSWORD 覆盖），数据库名 suisui
- JWT 过期：7天
- 图片目录：`./uploads/images/`

---

## 二、Android 端改动记录

### 2.1 Mock → 真实 API（12个文件）
全部 UI 页面从 `MockDataProvider` 改为 Retrofit API 调用：
- `LoginActivity.java` - 注册/登录调真实接口
- `MyWorldFragment.java` - 碎碎念加载/发布/删除/点赞
- `MomentDetailActivity.java` - 详情/评论/点赞/图片浏览
- `DiscoverFragment.java` - 发现页
- `OtherWorldActivity.java` - 他人世界
- `MeFragment.java` - 个人信息/公开开关
- `EditProfileActivity.java` - 编辑资料/图片上传
- `AccountSettingsActivity.java` - 改密码/退出
- `NotificationActivity.java` - 通知列表
- `StatisticsActivity.java` - 数据统计
- `SubscriptionManageActivity.java` - 订阅管理
- `BlacklistManageActivity.java` - 黑名单管理

### 2.2 Bug 修复记录

| 问题 | 原因 | 修复 |
|------|------|------|
| 登录失败显示"网络错误" | 400 响应的 errorBody 没解析 | 解析 errorBody JSON 获取服务器错误消息 |
| 用户名为"用户" | 创建 Moment 时没设 userNickname/UserAvatar | 补上 setUserNickname/setUserAvatar |
| 按钮太小显示不全 | 订阅/编辑按钮 32dp 高度不够 | 改为 40dp，字号 sm→md |
| 背景图编辑按钮 | 多余且功能重复 | 移除，统一走编辑资料页 |
| 图片上传不工作 | content:// URI 当 File 用 | 用 ContentResolver 读字节→写临时文件→上传 |
| 详情页头像不显示 | displayMoment 没用 ImageLoader | 加 ImageLoader.loadAvatar() |
| 时间显示 1970年 | API 返回字符串 createdAt 未解析 | TimeUtils.parseServerDate() 转换 |
| 详情页图片不显示 | ViewPager2 没设 Adapter | 创建 ImagePagerAdapter + item_full_image.xml |
| 点击图片闪退 | ViewPager2+NestedScrollView 冲突 | setNestedScrollingEnabled(false) + try-catch |
| 发现页头像偏移 | item_world_card 负边距太大 | -28dp→0dp |
| 编辑资料键盘遮挡 | ScrollView 撑满不弹 | layout_weight=1 + adjustResize |
| 编辑资料缺保存按钮 | btn_save 在标题栏不明显 | 改为底部固定全宽蓝色按钮 |
| 保存资料后不同步 | 返回 Fragment 不刷新 | MeFragment/MyWorldFragment 加 onResume 刷新 |

### 2.3 新增功能

| 功能 | 文件 |
|------|------|
| 全屏图片预览 | `ImagePreviewActivity.java`（黑色背景、可滑动、点击关闭） |
| ViewPager2 图片适配器 | `ImagePagerAdapter.java` |
| 全屏图片布局 | `item_full_image.xml`（FrameLayout + fitCenter ImageView） |
| 详情页图片自适应高度 | 单图 400dp，多图 250dp |
| 主页图片点击直接预览 | 不跳详情，直接打开全屏预览 |

### 2.4 网络配置
- 服务端地址不再硬编码：Android 端在 `local.properties` 里配置 `suisui.serverUrl`，构建时注入 `BuildConfig.BASE_URL`
- `ImageLoader.BASE_URL` 直接复用 `ApiClient.BASE_URL`，两处不会再出现不一致

---

## 三、关键踩坑

1. **MySQL JDBC 编码**：`characterEncoding=utf-8mb4` 非法，应为 `UTF-8`
2. **端口占用**：`start.bat` 自动检测并 kill 旧进程
3. **Maven 离线**：用 `java -jar` 替代 `mvn spring-boot:run`
4. **Android content:// URI**：不能直接 new File()，需 ContentResolver
5. **ViewPager2 在 NestedScrollView**：需 setNestedScrollingEnabled(false)
6. **Gson/Jackson 字段名**：`isLiked` 需 @JsonProperty
7. **Jackson null 序列化**：`spring.jackson.default-property-inclusion: non_null`
8. **模拟器图片选择器**：拖进去的图需用系统相机 App 触发扫描

---

## 四、项目文件结构

```
suisui/
├── app/                          # Android 端
│   ├── src/main/java/com/suisui/app/
│   │   ├── adapter/              # RecyclerView 适配器
│   │   ├── api/                  # Retrofit 接口 + Mock（已停用）
│   │   ├── model/                # 数据模型
│   │   ├── ui/                   # Activity/Fragment
│   │   │   ├── discover/         # 发现页、他人世界
│   │   │   ├── me/               # 我的、设置、统计等
│   │   │   └── world/            # 小世界、详情
│   │   └── util/                 # 工具类
│   └── src/main/res/             # 布局、资源
├── suisui-server/                # Spring Boot 服务端
│   ├── src/main/java/com/suisui/server/
│   │   ├── config/               # Security、CORS、Web配置
│   │   ├── controller/           # REST 控制器
│   │   ├── dto/                  # 请求/响应 DTO
│   │   ├── entity/               # JPA 实体
│   │   ├── exception/            # 异常处理
│   │   ├── repository/           # JPA Repository
│   │   ├── security/             # JWT 工具
│   │   └── service/              # 业务逻辑
│   ├── src/main/resources/
│   │   └── application.yml       # 配置文件
│   ├── uploads/images/           # 上传图片存储
│   ├── pom.xml
│   └── start.bat                 # 一键启动脚本
└── 项目开发记录.md
```
